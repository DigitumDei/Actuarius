services:
  actuarius:
    build:
      context: .
      args:
        GITHUB_TOKEN: ${GITHUB_TOKEN:-}
    image: actuarius:latest
    env_file:
      - .env
    volumes:
      - actuarius_data:/data
    restart: unless-stopped

volumes:
  actuarius_data:

