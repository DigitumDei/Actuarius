# AGENTS

## GitHub Interaction

- Prefer `gh` for GitHub operations in this repository.
- Use `gh` for author-sensitive actions such as creating pull requests, posting comments, and replying to review threads so those actions use the bot identity configured on this machine.
- Avoid the built-in GitHub connector for those author-sensitive actions unless explicitly requested.
